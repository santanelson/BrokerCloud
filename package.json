{
  "name": "brokercloud",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "install:all": "npm install && npm install --prefix api && npm install --prefix web",
    "deploy": "node deploy.js"
  }
}
